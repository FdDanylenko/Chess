import BoardComponent from "./components/BoardComponent";
// Винесена функція
function botFunction(): void {
  // Реалізація функції
}

// Розширення компоненту
(BoardComponent as any).botFunction = botFunction;